void main(void){
	int x;
	int y;
	int sum;
	PrivateTape.read(x);
	PrimaryTape.read(y);
	sum = x + y;
	
	PrimaryTape.seek(x, 5);
	PrivateTape.seek(y, 3);
	sum = sum + x;
	sum = sum + y;
	
	Prover.answer(sum);
}
