void main(void) {
	int i;
	int x;
	int y;
	int rounds;
	int[] K;
	rounds = 22;
	K = new int[rounds];
	i = 0;
	while (i < rounds) {
		PrimaryTape.read(x);
		K[i] = x;
		i++;
	}
	PrivateTape.read(x);
	PrivateTape.read(y);

	i = 0;
	while (i < rounds) {
		y = (y >> 7) | (y << 9);
        y += x;
        y ^= K[i];
        x = (x >> 14) | (x << 2);
        x ^= y;
		i++;
	}

	Prover.answer(y);
}
