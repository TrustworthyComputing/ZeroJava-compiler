void main(void) {
	int i;
	int key;
	int k;
	int val;
	int x;
	int size;
	PrimaryTape.read(size);
	PrimaryTape.read(key);
	i = 0;
	while (i < size) {
		PrivateTape.read(k);
		PrivateTape.read(val);
		if (k == key) {
			Prover.answer(val);
		}
		i++;
	}
	Prover.answer(0);
}
