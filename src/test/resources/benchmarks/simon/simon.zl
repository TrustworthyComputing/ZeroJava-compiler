void main(void) {
	int i;
	unsigned short x;
	unsigned short y;
	unsigned short tmp_x;
	unsigned short tmp_ror;
	int rounds;
	int[] K;
	rounds = 32;
	K = new int[rounds];
	i = 0;
	while (i < rounds) {
		PrimaryTape.read(x);
		K[i] = x;
		i++;
	}
	PrivateTape.read(x);
	PrivateTape.read(y);

	i = 0;
	while (i < rounds) {
		tmp_x = x;
		x = (tmp_x >> 15) | (tmp_x << 1);
		tmp_ror = (tmp_x >> 8) | (tmp_x << 8);
		x &= tmp_ror;
		x ^= y;
		tmp_ror = (tmp_x >> 14) | (tmp_x << 2);
		x ^= tmp_ror;
		x ^= K[i];
		y = tmp_x;
		i++;
	}
	Out.print(x);
	Out.print(y);
	
	Prover.answer(y);
}
