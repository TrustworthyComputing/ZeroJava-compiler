void main(void) {
	int i;
	int j;
	int arrj;
	int x;
	int key;
	int N;
	int[] arr;
	PrimaryTape.read(N);
	arr = new int[N];
	i = 0;
	while (i < N) {
		PrivateTape.read(x);
		arr[i] = x;
		i++;
	}

	i = 1;
	while (i < N) {
		key = arr[i]; 
		j = i - 1; 
		arrj = arr[j];
		while ((j >= 0) && (arrj > key)) { 
			arr[j + 1] = arr[j];
			j--;
			arrj = arr[j];
		}
		
		arr[j + 1] = key; 
		i++;
	}

	i = 0;
	while (i < N) {
		printf("%d\n",arr[i]);
		i++;
	}
	Prover.answer(arr[0]);
}
